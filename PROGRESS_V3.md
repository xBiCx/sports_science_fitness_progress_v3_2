# Sports Science Fitness Club — Progress Version 3

## อัปเดตแก้ไข Version 3.1

- แก้ข้อความบนปุ่มพื้นขาวใน Hero ที่หายเพราะสีตัวอักษรชนกับพื้นหลัง
- นำภาพสถานที่จริงชุดเดิม `fitness-overview-1.jpg` ถึง `fitness-overview-3.jpg` กลับมาใช้ในหน้า `fitness.html`
- เปลี่ยนข้อความหน้า Fitness ให้ตรงกับภาพพื้นที่จริง ไม่ใช้ข้อความภาพประกอบหรือภาพ Stock
- เปลี่ยนรายการใบรับรองจากเครื่องหมายถูกธรรมดาเป็นรายการที่กดตรวจสอบรายละเอียดได้
- ใบรับรองของเทรนเนอร์ตัวอย่างแสดงสถานะ “รอผู้ดูแลตรวจสอบ” จนกว่าจะมีไฟล์จริง หน่วยงานผู้ออก และเลขที่ใบรับรอง

## อัปเดตแก้ไข Version 3.2

- แก้ปุ่ม “ค้นหาเทรนเนอร์” ใต้ส่วนแพ็กเกจหน้าแรก จากสไตล์ตัวอักษรขาวสำหรับ Hero เป็นปุ่มขอบน้ำเงินสำหรับพื้นขาว
- ตรวจตำแหน่งที่ใช้ `btn-ghost-blue` ทั้งหมดแล้ว เหลือใช้เฉพาะปุ่มบนพื้นหลัง Hero สีน้ำเงินเท่านั้น

เวอร์ชันนี้พัฒนาต่อจาก Progress Version 2.1 โดยยังคงเป็น Front-end Demo และใช้ `localStorage` เพื่อเปิดทดลองได้ทันทีโดยไม่ต้องติดตั้งฐานข้อมูล

## ฟีเจอร์ที่เพิ่ม

### 1. Google OAuth

- หน้า `login.html` มีปุ่มเข้าสู่ระบบด้วย Google
- ค่าเริ่มต้นเป็น Demo Mode เพื่อสาธิต user flow โดยไม่ส่งข้อมูลออกจากเครื่อง
- เตรียมโค้ด Google Identity Services ไว้แล้ว เมื่อต้องการเชื่อมของจริงให้ใส่ Web Client ID ใน `js/oauth-config.js`
- หลัง Google Login ครั้งแรก ระบบพาไปหน้า Health Profile

> Production ต้องตรวจสอบ Google ID token ซ้ำที่ Backend และใช้ HTTPS ห้ามเชื่อข้อมูลจากการถอด JWT ฝั่ง Front-end เพียงอย่างเดียว

### 2. แพ็กเกจ 2 ประเภท

- `packages.html` แยกเป็น **แพ็กเกจฟิตเนส** 4 แบบ และ **แพ็กเกจเทรนเนอร์** 3 แบบ
- สมาชิกสลับหมวด เปรียบเทียบราคา และเลือกแพ็กเกจได้
- แพ็กเกจเทรนเนอร์ผูกกับเทรนเนอร์ที่เลือก

### 3. ระบบเทรนเนอร์

- `trainers.html` แสดงเทรนเนอร์ตัวอย่าง 6 คน
- ค้นหาจากชื่อหรือความเชี่ยวชาญ
- กรองตามความเชี่ยวชาญ เป้าหมาย และเพศเทรนเนอร์
- ใช้ Health Profile ช่วยจัดลำดับและแสดงป้าย “เหมาะกับเป้าหมายคุณ”
- `trainer-detail.html` แสดงประวัติ ความเชี่ยวชาญ ใบรับรอง คะแนน ประสบการณ์ และช่วงเวลาที่ให้บริการ
- เลือกเทรนเนอร์แล้วไปเลือกแพ็กเกจต่อได้

### 4. Health Profile และการคำนวณ

- `health-profile.html` รับอายุ เพศ ส่วนสูง น้ำหนัก Body Fat ระดับกิจกรรม ประสบการณ์ เป้าหมาย และข้อจำกัด
- คำนวณ BMI, BMR, TDEE, Daily Calories เป้าหมาย และโปรตีนต่อวัน
- บันทึกข้อมูลแยกตามอีเมลสมาชิกใน `localStorage`
- ส่งเป้าหมายไปใช้แนะนำเทรนเนอร์
- Dashboard แสดงผลสุขภาพย่อและเทรนเนอร์ที่เลือก

## วิธีทดลอง

1. เปิด `index.html`
2. ไปหน้า `login.html` แล้วกด “เข้าสู่ระบบด้วย Google” เพื่อทดลอง Demo OAuth
3. กรอกข้อมูลที่ `health-profile.html` แล้วกดคำนวณ
4. กด “ค้นหาเทรนเนอร์ที่เหมาะกับฉัน”
5. ทดลอง Filter และเปิดโปรไฟล์เทรนเนอร์
6. เลือกเทรนเนอร์และแพ็กเกจ
7. กลับ `dashboard.html` เพื่อดูข้อมูลสรุปและส่งคำขอจองตามระบบเดิม

## เชื่อม Google OAuth จริง

1. สร้าง OAuth 2.0 Web Client ใน Google Cloud Console
2. เพิ่มโดเมนที่ใช้งานใน Authorized JavaScript origins
3. ใส่ Client ID ใน `js/oauth-config.js`
4. เปิดเว็บผ่าน `http://localhost` หรือ HTTPS ไม่ควรเปิดด้วย `file://`
5. ทำ Backend สำหรับตรวจสอบ ID token และสร้าง session ที่ปลอดภัยก่อนใช้งานจริง

## ขอบเขตที่ยังเป็น Demo

- ข้อมูลและรายการจองเก็บในเบราว์เซอร์
- รายชื่อ/ตารางเทรนเนอร์เป็นข้อมูลตัวอย่าง
- การเลือกแพ็กเกจยังไม่มีการชำระเงินจริง
- ยังไม่มี Backend, Database, Admin approval และระบบ session จริง


แก้ไขเพิ่มเติม: ใช้สถานะเข้าสู่ระบบจริงเท่านั้น ไม่ใช้ข้อมูลสมัครสมาชิกแทน session; ล้าง session ทั้งสองแบบเมื่อออกจากระบบ; รีเฟรชสถานะเมื่อย้อนกลับและเปลี่ยนแท็บ; ป้องกันเปิด Dashboard หลังออกจากระบบ; ดูโปรไฟล์/แพ็กเกจไม่เลือกโค้ชและไม่บันทึกโค้ชจาก URL อัตโนมัติ
